/* this is multiline comment */
int main() {
  char  a;
  char *b;  // this is single line comment
  char  c;

  int   d;
  int  *e;
  int   f;

  a= 97; printchar(a);
  b= &a; c= *b; printchar(c);

  d= 12; printint(d);
  e= &d; f= *e; printint(f);
  return(0);
}
