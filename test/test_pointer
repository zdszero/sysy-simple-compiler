/* this is multiline comment */
int main() {
  char  a;
  char *b;  // this is single line comment
  char  c;

  int   d;
  int  *e;
  int   f;

  a= 97; putch(a);
  b= &a; c= *b; putch(c);

  d= 12; putint(d);
  e= &d; f= *e; putint(f);
  return(0);
}
