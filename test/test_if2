void main() {
    int a = 3;
    int b = 5;
    if (a > 2) {
        if (a > 4) {
            printint(a + 10);
        } else {
            printint(a * 2);
        }
    } else {
        printint(b);
    }
}
