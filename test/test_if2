void main() {
    int a = 3;
    int b = 5;
    if (a > 2) {
        if (a > 4) {
            putint(a + 10);
        } else {
            putint(a * 2);
        }
    } else {
        putint(b);
    }
}
